# Next Approved Steps

1. Use the tracking protocol for each human-requested task: read context and status, create a task event, review diffs, and record verification truthfully.

Phase 4 is verified: one fresh ultra-flat Generate Content request completed and persisted safely. The finished run contains only reserve candidates because none crossed the configured quality threshold; the system did not fabricate selected clips. Do not begin Phase 5 rendering or any later phase without a separate explicit authorization.

Phase 3.1 OpenAI configuration is present in the ignored local API environment file. Three authorized job runs were rate limited. Do not retry OpenAI. Do not add any API values to tracked files or chat.

The verified checkpoint has been published to the private `codex-live` branch. Do not merge, force-push, publish publicly, or enable unattended synchronization.
