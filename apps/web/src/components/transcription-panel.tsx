"use client";

import { useCallback, useEffect, useState } from "react";

type Locale = "en" | "ar";
type Video = { id: string; duration_seconds: number; original_filename: string };
type Capabilities = { configuration: { transcription_provider_available: boolean; openai_api_key_configured: boolean; transcription_model_configured: boolean; cost_estimation_configured: boolean } };
type Job = { id: string; state: string; current_stage: string; progress_percent: number | null; progress_indeterminate: boolean; estimated_cost_usd: number | null; cost_approval_required: boolean; retry_available: boolean; cancellation_available: boolean; error_code: string | null };
type Transcript = { detected_language: string | null; full_text: string; duration_seconds: number; provider: string; model: string; segments: { index: number; start_seconds: number; end_seconds: number; text: string; confidence: number | null }[] };

const api = (process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://127.0.0.1:8000").replace(/\/$/, "");
const labels = { en: { heading: "Transcription", automatic: "Automatic", arabic: "Arabic", english: "English", start: "Extract audio and transcribe", configured: "OpenAI transcription configured", notConfigured: "OpenAI transcription is not configured on this device.", cost: "Estimated cost", approval: "Approval is required before the provider call.", approve: "Approve estimated cost", cancel: "Cancel job", retry: "Retry job", extracting: "Extracting audio", transcribing: "Transcribing", done: "Transcription complete. Clip selection will be added in the next phase.", unavailable: "Transcript unavailable", provider: "Provider" }, ar: { heading: "\u0627\u0644\u062a\u0641\u0631\u064a\u063a", automatic: "\u062a\u0644\u0642\u0627\u0626\u064a", arabic: "\u0627\u0644\u0639\u0631\u0628\u064a\u0629", english: "English", start: "\u0627\u0633\u062a\u062e\u0631\u062c \u0627\u0644\u0635\u0648\u062a \u0648\u0641\u0631\u0651\u063a", configured: "\u062a\u0641\u0631\u064a\u063a OpenAI \u0645\u0647\u064a\u0623", notConfigured: "\u0644\u0645 \u064a\u062a\u0645 \u0625\u0639\u062f\u0627\u062f \u062a\u0641\u0631\u064a\u063a OpenAI \u0639\u0644\u0649 \u0647\u0630\u0627 \u0627\u0644\u062c\u0647\u0627\u0632.", cost: "\u0627\u0644\u062a\u0643\u0644\u0641\u0629 \u0627\u0644\u0645\u062a\u0648\u0642\u0639\u0629", approval: "\u062a\u0644\u0632\u0645 \u0645\u0648\u0627\u0641\u0642\u0629 \u0635\u0631\u064a\u062d\u0629 \u0642\u0628\u0644 \u0627\u062a\u0635\u0627\u0644 \u0645\u0632\u0648\u062f \u0627\u0644\u062e\u062f\u0645\u0629.", approve: "\u0648\u0627\u0641\u0642 \u0639\u0644\u0649 \u0627\u0644\u062a\u0643\u0644\u0641\u0629", cancel: "\u0623\u0644\u063a \u0627\u0644\u0645\u0647\u0645\u0629", retry: "\u0623\u0639\u062f \u0627\u0644\u0645\u062d\u0627\u0648\u0644\u0629", extracting: "\u064a\u062a\u0645 \u0627\u0633\u062a\u062e\u0631\u0627\u062c \u0627\u0644\u0635\u0648\u062a", transcribing: "\u064a\u062a\u0645 \u0627\u0644\u062a\u0641\u0631\u064a\u063a", done: "\u0627\u0643\u062a\u0645\u0644 \u0627\u0644\u062a\u0641\u0631\u064a\u063a. \u0633\u064a\u0643\u0648\u0646 \u0627\u062e\u062a\u064a\u0627\u0631 \u0627\u0644\u0645\u0642\u0627\u0637\u0639 \u0641\u064a \u0627\u0644\u0645\u0631\u062d\u0644\u0629 \u0627\u0644\u0642\u0627\u062f\u0645\u0629.", unavailable: "\u0627\u0644\u062a\u0641\u0631\u064a\u063a \u063a\u064a\u0631 \u0645\u062a\u0627\u062d", provider: "\u0627\u0644\u0645\u0632\u0648\u062f" } } as const;

export function TranscriptionPanel({ video, locale }: { video: Video; locale: Locale }) {
  const [capabilities, setCapabilities] = useState<Capabilities | null>(null); const [language, setLanguage] = useState("auto"); const [job, setJob] = useState<Job | null>(null); const [transcript, setTranscript] = useState<Transcript | null>(null); const [error, setError] = useState<string | null>(null); const t = labels[locale];
  const getJob = useCallback(async (jobId: string) => { const response = await fetch(`${api}/api/jobs/${jobId}`, { cache: "no-store" }); if (!response.ok) throw new Error("job_unavailable"); const current = await response.json() as Job; setJob(current); return current; }, []);
  const getTranscript = useCallback(async () => { const response = await fetch(`${api}/api/videos/${video.id}/transcript`, { cache: "no-store" }); if (response.ok) setTranscript(await response.json() as Transcript); }, [video.id]);
  useEffect(() => { void (async () => { try { const [capabilityResponse, jobsResponse] = await Promise.all([fetch(`${api}/api/system/capabilities`), fetch(`${api}/api/videos/${video.id}/jobs`, { cache: "no-store" })]); if (capabilityResponse.ok) setCapabilities(await capabilityResponse.json() as Capabilities); if (jobsResponse.ok) { const jobs = await jobsResponse.json() as Job[]; if (jobs[0]) { setJob(jobs[0]); if (jobs[0].state === "completed") await getTranscript(); } } } catch { setError("network_error"); } })(); }, [getTranscript, video.id]);
  useEffect(() => { if (!job || ["completed", "failed", "cancelled", "awaiting_cost_approval"].includes(job.state)) return; const interval = window.setInterval(() => { void getJob(job.id).then((current) => { if (current.state === "completed") void getTranscript(); }).catch(() => setError("network_error")); }, 1000); return () => window.clearInterval(interval); }, [getJob, getTranscript, job]);
  const create = async () => { setError(null); const response = await fetch(`${api}/api/videos/${video.id}/transcription-jobs`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ language_mode: language, provider: "openai", approve_estimated_cost: false }) }); if (!response.ok) { const body = await response.json().catch(() => null) as { detail?: { code?: string } } | null; setError(body?.detail?.code ?? "transcription_failed"); return; } setJob(await response.json() as Job); };
  const action = async (route: "approve-cost" | "cancel" | "retry") => { if (!job) return; const response = await fetch(`${api}/api/jobs/${job.id}/${route}`, { method: "POST" }); if (response.ok) setJob(await response.json() as Job); else setError("transcription_failed"); };
  const configured = capabilities?.configuration?.transcription_provider_available === true;
  return <section className="mt-8 rounded-2xl border border-[var(--line)] p-5" aria-live="polite"><h2 className="text-xl font-bold">{t.heading}</h2><p className="mt-2 text-sm">{configured ? t.configured : t.notConfigured}</p><p className="mt-2 text-sm">{t.provider}: OpenAI · {video.duration_seconds.toFixed(1)} s</p>{!job && <><label className="mt-4 block text-sm" htmlFor="language">{t.automatic}<select id="language" value={language} onChange={(event) => setLanguage(event.target.value)} className="ms-2 rounded border p-2"><option value="auto">{t.automatic}</option><option value="ar">{t.arabic}</option><option value="en">{t.english}</option></select></label><button type="button" disabled={!configured} onClick={() => void create()} className="mt-4 rounded bg-[var(--accent)] px-4 py-2 text-white disabled:opacity-50">{t.start}</button></>}{job && <div className="mt-4"><p>{job.current_stage}</p>{job.current_stage === "extracting_audio" && <progress aria-label="Audio extraction progress" value={job.progress_percent ?? 0} max="100" className="w-full" />}{job.progress_indeterminate && <p><progress aria-label="Transcription in progress" /> {t.transcribing}</p>}{job.estimated_cost_usd !== null && <p>{t.cost}: ${job.estimated_cost_usd.toFixed(4)}</p>}{job.cost_approval_required && <><p>{t.approval}</p><button type="button" onClick={() => void action("approve-cost")} className="rounded border p-2">{t.approve}</button></>}{job.cancellation_available && <button type="button" onClick={() => void action("cancel")} className="m-2 rounded border p-2">{t.cancel}</button>}{job.retry_available && <button type="button" onClick={() => void action("retry")} className="m-2 rounded border p-2">{t.retry}</button>}{job.error_code && <p role="alert" className="text-rose-500">{job.error_code}</p>}</div>}{error && <p role="alert" className="mt-3 text-rose-500">{error}</p>}{transcript && <article className="mt-5 rounded border p-4"><p className="font-semibold">{t.done}</p><p className="mt-3 whitespace-pre-wrap break-words" dir="auto">{transcript.full_text}</p><p className="mt-2 text-sm">{transcript.provider} / {transcript.model}</p><ol className="mt-3 space-y-2">{transcript.segments.map((segment) => <li key={segment.index} dir="auto">{segment.start_seconds.toFixed(2)}–{segment.end_seconds.toFixed(2)} · {segment.text}</li>)}</ol></article>}</section>;
}
